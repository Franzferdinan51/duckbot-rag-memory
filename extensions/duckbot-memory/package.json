{
  "name": "duckbot-memory",
  "version": "0.1.0",
  "description": "OpenClaw native plugin that wires the duckbot-rag-memory brain (64 MCP tools + session_start/session_end hooks) into OpenClaw. Pure Node.js shim — spawns the Python MCP server as a subprocess and proxies tool calls over JSON-RPC over stdio. Zero npm dependencies; uses Node stdlib only.",
  "main": "index.js",
  "openclaw": {
    "manifest": "./openclaw.plugin.json"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT",
  "private": true
}